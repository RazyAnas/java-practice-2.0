public class StaticVariablesVSInstanceVariables {
    /* static variables are declared by using keyword, static variables are also known as static member variables.
    Every instance of the class shares the same variable
    If changes are made to that variable, all other instances of that class will see the effect of that change.

    Each instance has its own copy of an instance variable.
    Every instance can have a different value.
     */
}
