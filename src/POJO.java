public class POJO {
    // POJO stands for Plain Old Java Object. Generally has instance fields only. It is used to house data and pass data between functional classes.
    // it usually has no other or very few  methods other than getters and setters for the instance fields
    // many database frameworks use POJO to read data from, or to write data tot databases, files or streams.
}
